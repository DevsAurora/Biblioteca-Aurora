<!DOCTYPE html>
<html>
<head>
  <title><%= livro ? "Editar Livro" : "Novo Livro" %></title>
</head>
<body>
  <h1><%= livro ? "Editar Livro" : "Novo Livro" %></h1>

  <form action="<%= livro ? '/livros/' + livro.id + '/update' : '/livros' %>" method="POST">
    <label for="titulo">Título:</label>
    <input type="text" id="titulo" name="titulo" value="<%= livro ? livro.titulo : '' %>" required>

    <label for="autor">Autor:</label>
    <input type="text" id="autor" name="autor" value="<%= livro ? livro.autor : '' %>" required>

    <label for="ano_publicacao">Ano:</label>
    <input type="number" id="ano_publicacao" name="ano_publicacao" value="<%= livro ? livro.ano_publicacao : '' %>" required>

    <label for="isbn">ISBN:</label>
    <input type="text" id="isbn" name="isbn" value="<%= livro ? livro.isbn : '' %>" required>
    
    <label for="genero">Gênero:</label>
    <input type="text" id="genero" name="genero" value="<%= livro ? livro.genero : '' %>">
    
    <button type="submit"><%= livro ? "Atualizar" : "Criar" %></button>
  </form>

  <a href="/livros">Voltar para lista</a>
</body>
</html>