<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= usuario.id ? "Editar Usuário" : "Criar Usuário" %></title>
</head>
<body>
  <h1><%= usuario.id ? "Editar Usuário" : "Criar Usuário" %></h1>

  <nav></nav>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/livros">Livros</a></li>
      <li><a href="/usuarios">Usuários</a></li>
    </ul>
  </nav>

  <form 
    action="<%= usuario.id ? '/usuarios/' + usuario.id + '/update' : '/usuarios' %>" 
    method="POST"
  >
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="<%= usuario.nome || '' %>" required>

    <label for="sobrenome">Sobrenome:</label>
    <input type="text" id="sobrenome" name="sobrenome" value="<%= usuario.sobrenome || '' %>">

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" value="<%= usuario.email || '' %>" required>

    <label for="senha">Senha:</label>
    <input type="password" id="senha" name="senha" <%= usuario.id ? "" : "required" %>>

    <label for="role">Perfil:</label>
    <select id="role" name="role">
      <option value="ALUNO" <%= usuario.role === "ALUNO" ? "selected" : "" %>>Aluno</option>
      <option value="BIBLIOTECARIO" <%= usuario.role === "BIBLIOTECARIO" ? "selected" : "" %>>Bibliotecário</option>
      <option value="PROFESSOR" <%= usuario.role === "PROFESSOR" ? "selected" : "" %>>Professor</option>
    </select>

    <button type="submit"><%= usuario.id ? "Atualizar" : "Criar" %></button>
  </form>

  <% if (usuario.id) { %>
    <form action="/usuarios/<%= usuario.id %>/delete" method="POST" style="margin-top:20px;">
      <button type="submit">Excluir Usuário</button>
    </form>
  <% } %>
</body>
</html>